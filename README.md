
  # Babywatch

  This is a code bundle for Babywatch. The original project is available at https://www.figma.com/design/LoMgyC5NqBCCb62g3XHm3l/Babywatch.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  